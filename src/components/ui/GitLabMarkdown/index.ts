export type {
  GitLabMarkdownProps,
  GitLabRefContext,
  GitLabRefMatch,
  RemarkGitLabRefsOptions,
} from './types';

export { extractProjectPath, extractInstanceUrl } from './utils';
export { remarkGitLabRefs } from './remarkGitLabRefs';
export { GitLabMarkdown } from './GitLabMarkdown';
